tries = 0
guess = 0

while guess != 6    and tries   <=   3:
  guess = int(input("Guess the number:  "))
  tries +=  1
  
if  tries   >  3:
    print("too many tries") 
else:
    print("you got it")

